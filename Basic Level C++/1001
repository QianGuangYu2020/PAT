#include <stdio.h>
int main(){
    int ans=0;
    int num;
    scanf("%d", &num);
    while(num!=1){
        if(num%2==0){
            num/=2;
            ans++;
        }else{
            num=(num*3+1)/2;
            ans++;
        }
    }
    printf("%d", ans);
    return 0;
}
