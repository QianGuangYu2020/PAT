#include <iostream>
#include <cmath>
using namespace std;
int main(){
    int n;
    cin>>n;
    double a, b, max=0;
    for(int i=0; i<n; i++){
        cin>>a>>b;
        if(a*a+b*b>max) max=a*a+b*b;
    }
    printf("%.2f", sqrt(max));
    return 0;
}
